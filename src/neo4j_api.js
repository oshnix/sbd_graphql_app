let Api = {
    /**
     *
     * @param {string} params
     * @returns {null}
     */
    returnHumanConnections(params){
        return null;
    },
    addHuman(){

    },
    addHumans(){

    },
    addCountries(param){
        if(Array.isArray(param)){

        } else {

        }
    }
};

module.exports = Api;